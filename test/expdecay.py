import ual
import numpy

# Get rho length
#rho_size = len(coreprof0(0).rho_tor)

# Allocate coresource
coresource0 = ual.coresource.coresourceArray()
coresource0.resize(1)
##coresource0.resize(len(coreprof0.array))
#coresource0.values.resize(1)


# Source description
#coresource0.values(0).sourceid.id = 'expdecay" 
#coresource0.values(0).sourceid.flag = coresource0.values.sourceid.flag  
#coresource0.values(0).sourceid.description = 'Exponential heat drain'

# Filling up data

#for i in range(0, len(coreprof0.rho_tor)-1)
#   coresource0.values(0).rho_tor(i) = coreprof0.rho_tor(i) 
#   coresource0.values(0).j(i) = 0
#   coresource0.values(0).qe.exp(i) = 0
#   coresource0.values(0).qe.imp(i) = coreprof0.te/1.6e-19 # hack
#   coresource0.values(0).se.exp(i) = 0
#   coresource0.values(0).se.imp(i) = 0

